import express from "express";
// import { paypalConfig } from "../controllers/controllers.js";
const paypalRoute = express.Router();
// paypalRoute.get("/config/paypal", paypalConfig);
export default paypalRoute;
